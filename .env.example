# Production domain and brand (canonical URLs, sitemap, metadata)
NEXT_PUBLIC_SITE_URL=https://www.devtooldock.com
NEXT_PUBLIC_SITE_NAME=DevToolDock

# Get a key from https://platform.openai.com/api-keys — never commit real values
OPENAI_API_KEY=
